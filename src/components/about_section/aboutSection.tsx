import React from 'react';
import Section from '../section/section';
import SectionNames from '../../universal/constants/sectionNames';

const AboutSection = () => {
    return (
        <Section classNm={ SectionNames.ABOUT } />
    )
}

export default AboutSection;
